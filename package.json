{
  "name": "site-formation",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "build": "npm run build -w backend && npm run build -w frontend",
    "check": "npm run typecheck -w backend && npm run build -w frontend && npm run smoke -w backend"
  }
}
